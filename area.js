// Fórmula para calcular a área de uma circunferência (A = Pi * raio * raio)
let raio = 10
console.log(`Área: ${Math.PI * raio * raio} m2`) //para reduzir as casas decimais basta usar o toFixed(2). Ex: raio.toFixed(2)